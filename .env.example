# Notion Integration (choose one method)
# Option 1: Direct API token (portable, for external deployments)
NOTION_TOKEN=
# Option 2: Replit Connectors (automatic when using Replit)

# Notion Database IDs
NOTION_NEWSLETTER_DATABASE_ID=
NOTION_CONTACTS_DATABASE_ID=

# CORS (comma-separated list of allowed origins for split deployments)
CORS_ORIGINS=

# Frontend API base URL (only needed for split deployments)
VITE_API_BASE_URL=
